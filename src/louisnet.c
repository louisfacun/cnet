#include "matrix.h"
#include "activation.h"

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

int main(void)
{
   
    //clock_t tic = clock();
    //printf("%f", sigmoid(1));
    //printf("%f", relu(-0.55));
    //clock_t toc = clock();
    //printf("\nElapsed: %f seconds\n", ((double)(toc - tic) / CLOCKS_PER_SEC));

    /*
    Matrix *matrix1 = create_zero_matrix(3, 2);
    fill_num_matrix(matrix1);
    print_matrix(matrix1);
    printf("\n");

    Matrix *matrix2 = create_zero_matrix(2, 2);
    fill_num_matrix(matrix2);
    print_matrix(matrix2);
    printf("\n");

    Matrix *mat_product = multiply_matrix(matrix1, matrix2);
    print_matrix(mat_product);

    free_matrix(matrix1);
    free_matrix(matrix2);
    free_matrix(mat_product);*/
}